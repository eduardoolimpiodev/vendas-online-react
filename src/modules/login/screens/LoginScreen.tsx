import { BackGroundImage, ContainerLogin } from '../styles/loginScreen.styles';

const LoginScreen = () => {
  return (
    <div>
      <BackGroundImage src="./bg.png" />
      <ContainerLogin />
    </div>
  );
};

export default LoginScreen;
